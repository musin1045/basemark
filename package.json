{
  "name": "basemark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BaseMark local-first foundation",
  "scripts": {
    "test": "node --test --test-isolation=none",
    "cli": "node src/cli/basemarkCli.js",
    "app": "node src/server/basemarkServer.js",
    "soak": "node scripts/soak_local_app.mjs",
    "android:sync": "npx cap sync android",
    "android:run": "npx cap run android"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "konva": "^10.2.3"
  }
}
